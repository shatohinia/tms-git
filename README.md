# tms-git
 -Клонировал репозиторий с помощью команды git clone
 -Сделал файлы отслеживаемыми с помошью команды git add
 -Посмотрел изменения в .git/objects(index) с помощью команды ls -l
 -Cделал коммит git commit -m
 -Посмотрел хэш коммита git log -1
 -Исправиk опечатку в index.html c помощью редактора nano
 -Добавил изменения в индех git add -m
 -Добавил в репозиторий с помощью команды git push origin main
